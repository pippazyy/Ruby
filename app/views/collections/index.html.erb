<p id="notice"><%= notice %></p>

<h1><%= @username %>'s Collections</h1>

<table>
  <thead>
    <tr>
      <th>Album</th>
      <th>Artist</th>
      <th>Song</th>
      <th>Favorite</th>
      <!-- <th>User</th> -->
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @collections.each do |collection| %>
      <tr>
        <td><%= collection.album %></td>
        <td><%= collection.artist %></td>
        <td><%= collection.song %></td>
        <td><%= collection.favorite %></td>
        <!-- <td><%= collection.user_id %></td> -->
        <!-- <td><%= link_to 'Show', collection %></td>
        <td><%= link_to 'Edit', edit_collection_path(collection) %></td>
        <td><%= link_to 'Destroy', collection, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Collection', new_collection_path %>
